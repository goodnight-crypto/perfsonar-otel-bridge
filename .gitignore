.env
# .env の派生（.env.local 等）とエディタの swap ファイル。
# swap には編集中のトークンが残りうるため .env と同じ扱いで除外する
.env.*
!.env.example
*.swp
*.swo
*~

__pycache__/
*.pyc
.DS_Store
.venv/

# ローカル専用（public 化にあたり履歴から除去済み。2026-08-08）
# CLAUDE.md は Claude Code が作業ディレクトリから読むので、消さずに追跡だけ外す
CLAUDE.md
docs/review/
